{
  "name": "hyperdash-info",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Zero-dep stdio MCP for Hyperdash public GraphQL reads — expanded coverage (deltas, liq levels, widgets, trader stats)"
}
